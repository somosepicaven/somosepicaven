rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Ecosistema ÉPICA - Reglas de Seguridad Cero Papel
    match /artifacts/{appId}/public/{document=**} {
      allow read: if true; 
      allow write: if request.auth != null;
    }
  }
}
